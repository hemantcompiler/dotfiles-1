#!/bin/bash

mkdir -p ~/.local/bin/

ln -sv `pwd`/google ~/.local/bin/google
ln -sv `pwd`/tmux-right-side ~/.local/bin/tmux-right-side
ln -sv `pwd`/shared ~/.local/bin/shared
ln -sv `pwd`/update_pkg ~/.local/bin/update_pkg
