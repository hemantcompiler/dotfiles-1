conky.config = {

    background = false,
    update_interval = 0.5,

    double_buffer = true,
    no_buffers = true,

    text_buffer_size = 2048.0,

    -- Window specifications
    own_window = true,
    own_window_class = 'conky',
    own_window_type = 'override',
    own_window_transparent = false,
    own_window_colour = '2b303b',
    own_window_hints = 'undecorated,below,sticky,skip_taskbar,skip_pager',

    minimum_height = 1,
    minimum_width = 1366,
    maximum_width = 1366,

    border_inner_margin = 0,
    border_outer_margin = 0,
    border_width = 0,

    alignment = 'bottom_left',
    gap_x = 0,
    gap_y = 0,

    -- Graphics settings
    draw_shades = false,
    draw_outline = false,
    draw_borders = false,
    draw_graph_borders = false,


};

conky.text = [[
${color }$if_mpd_playing$mpd_bar$endif
]]
